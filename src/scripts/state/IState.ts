export interface IState {
  difficulty: string;
  wordsAmount: number;
  currPage: string;
  bookPage: number;
  animationType: string;
  lang: string;
  curLinks: string[];
  isAuth: boolean;
  colorScheme: string;
}
